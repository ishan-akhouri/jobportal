from django.urls import path
from . import views

app_name = 'jobs'  # Namespace for job-related URLs

urlpatterns = [
    # We'll add job-related URLs here later:
    # path('', views.job_list, name='job_list'),
    # path('<int:pk>/', views.job_detail, name='job_detail'),
    # etc.
]
